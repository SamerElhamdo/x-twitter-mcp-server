# ========================================
# Twitter MCP Server Configuration
# ========================================
# انسخ هذا الملف إلى .env وقم بتعديل القيم

# ========================================
# Twitter OAuth 2.0 Configuration
# ========================================
# احصل على هذه القيم من https://developer.twitter.com/
# 1. اذهب إلى Twitter Developer Portal
# 2. أنشئ تطبيق جديد أو استخدم موجود
# 3. فعّل OAuth 2.0
# 4. أضف Callback URL: http://YOUR_SERVER_IP:8000/auth/callback
# 5. انسخ Client ID و Client Secret

TWITTER_CLIENT_ID=your_twitter_client_id_here
TWITTER_CLIENT_SECRET=your_twitter_client_secret_here

# ========================================
# Twitter API v1.1 (للتوافق مع الكود القديم)
# ========================================
# هذه المتغيرات اختيارية للتوافق مع الكود القديم

TWITTER_API_KEY=your_twitter_api_key_here
TWITTER_API_SECRET=your_twitter_api_secret_here

# ========================================
# Server Configuration
# ========================================
# إعدادات الخادم الأساسية

# عنوان الخادم (استخدم 0.0.0.0 للوصول من الخارج)
HOST=127.0.0.1

# منفذ الخادم (عند التشغيل المباشر)
PORT=8000

# منفذ mcp-proxy (عند استخدام mcp-proxy)
MCP_PROXY_PORT=9000

# ========================================
# OAuth Callback Configuration
# ========================================
# يجب أن يتطابق هذا مع إعدادات تطبيق Twitter الخاص بك

# للاستخدام المحلي
TWITTER_REDIRECT_URI=http://127.0.0.1:8000/auth/callback

# للإنتاج (استبدل yourdomain.com باسم النطاق الخاص بك)
# TWITTER_REDIRECT_URI=https://yourdomain.com/auth/callback

# ========================================
# Security Configuration
# ========================================
# إعدادات الأمان

# مفتاح سري للجلسات (غير هذا في الإنتاج!)
SECRET_KEY=your-super-secret-key-change-this-in-production

# مفتاح سري إضافي (للتوافق مع الكود القديم)
API_SECRET_KEY=your-api-secret-key-here

# ========================================
# Database Configuration
# ========================================
# إعدادات قاعدة البيانات

# قاعدة البيانات المحلية (SQLite)
DATABASE_URL=sqlite:///./twitter_accounts.db

# للإنتاج (PostgreSQL - اختياري)
# DATABASE_URL=postgresql://username:password@localhost/twitter_mcp

# ========================================
# OAuth Security Settings
# ========================================
# إعدادات أمان OAuth

# مدة صلاحية حالة OAuth (بالثواني) - ساعة واحدة
OAUTH_STATE_EXPIRE_SECONDS=3600

# مدة صلاحية access token (بالدقائق) - ساعة واحدة
ACCESS_TOKEN_EXPIRE_MINUTES=60

# مدة صلاحية حالة OAuth (بالثواني) - ساعة واحدة
OAUTH_STATE_EXPIRE_SECONDS=3600

# ========================================
# Optional: Production Settings
# ========================================
# إعدادات إضافية للإنتاج

# تفعيل وضع الإنتاج
# ENVIRONMENT=production

# تسجيل الأخطاء
# LOG_LEVEL=INFO

# عدد العمال (للإنتاج)
# WORKERS=4

# ========================================
# Example for Production Server
# ========================================
# مثال لإعدادات خادم الإنتاج:

# HOST=0.0.0.0
# PORT=8000
# TWITTER_REDIRECT_URI=https://yourdomain.com/auth/callback
# SECRET_KEY=your-production-secret-key-here
# ENVIRONMENT=production
# LOG_LEVEL=INFO
# WORKERS=4
